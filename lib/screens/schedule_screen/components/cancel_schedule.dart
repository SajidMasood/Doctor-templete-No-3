import 'package:flutter/material.dart';

class CanceledSchedule extends StatelessWidget {
  const CanceledSchedule({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Canceled Widget"),
    );
  }
}
